{
  "dependencies": {
    "@quixo3/prisma-session-store": "^3.1.13",
    "connect-pg-simple": "^10.0.0",
    "dotenv": "^16.5.0",
    "ejs": "^3.1.10",
    "express": "^5.1.0",
    "express-session": "^1.18.1",
    "jest": "^29.7.0",
    "multer": "^1.4.5-lts.2",
    "nodemon": "^3.1.9",
    "passport": "^0.7.0",
    "passport-local": "^1.0.0"
  },
  "devDependencies": {
    "prisma": "^6.6.0"
  },
  "scripts": {
    "live":"node --watch app.js",
    "migrate": "npx prisma migrate dev --name init",
    "test": "jest",
    "testLive": "nodemon --exec 'jest' --watchAll"
  }
}
