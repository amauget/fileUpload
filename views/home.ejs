<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home Page</title>
</head>
<body>
    
    <%if(user.username){%>
        <h2 class="welcome">Welcome, <%=user.displayedUsername%>!</h2>
        <a href="/logout">Log Out</a>
        <a href="/upload">Upload Files</a>

        <%if(files.length === 0){%>
            <h3>You have no uploaded files </h3>
        <%}else{%>
            <h3>Your files:</h3>
        <%}%>

        <input type="text" class="search" placeholder="search your files" onkeyup=searchFiles()>
        <div class="fileContainer">
            <a id="file">apple</a>
            <a id="file">america</a>
            <a id="file">chess</a>
            <a id="file">Zebra</a>
            <a id="file">Baseball</a>
            <a id="file">file6</a>
            <a id="file">file7</a>
            <a id="file">file8</a>
            <a id="file">file9</a>
        </div>

    <%}else{%>
        <h1>Home</h1>
        <a href="/login">Login</a>
        <a href="/register">Register</a>
    <%}%>


    
</body>
<script>
    function searchFiles(){
        const files = document.querySelectorAll('#file')

        for(let i of files){
            const fileName = i.textContent.toLowerCase()

           if(fileName.includes(event.target.value.toLowerCase())){
                i.style.display = 'block'
           }
           else{
                i.style.display = 'none'
           }
        }
    }
</script>
</html>